#include "renderer/passes/PassPlugin.h"

void
Cel::Renderer::Passes::PassPlugin::build(SystemScheduler scheduler,
                                         ResourceManager& resourceManager)
{
    // Add pass recording to ecs schedule

    // Add passes to render graph
}
